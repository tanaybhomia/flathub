app-id: io.github.tanaybhomia.DeepDive
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: deepdive
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: deepdive
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tanaybhomia/DeepDive.git
        tag: v1.0.0
        commit: c0858604654873e34162b0b11a72a503c26ee87f
